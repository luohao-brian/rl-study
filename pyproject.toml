[project]
name = "rl-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0.0",
    "click>=8.0.0",
    "numpy>=1.26",
]

[project.scripts]
maze = "maze.main:cli"

[tool.uv]
package = true
